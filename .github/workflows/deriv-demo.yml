name: Deriv Demo Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

concurrency:
  group: deriv-demo-bot
  cancel-in-progress: false

jobs:
  demo:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-deriv.txt
      - name: Run Deriv demo bot
        env:
          DEMO_ONLY: "true"
          DRY_RUN: "false"
          DERIV_TOKEN: ${{ secrets.DERIV_TOKEN }}
          DERIV_APP_ID: ${{ secrets.DERIV_APP_ID }}
          DERIV_SYMBOL: "AUTO"
          STAKE_USD: "1"
          ACCU_GROWTH_RATE: "0.02"
          CLOSE_AFTER_SECONDS: "1"
          MAX_TRADES: "0"
          COOLDOWN_SECONDS: "3"
          STABLE_MARKETS_LIMIT: "5"
          MAX_DAILY_LOSS_USD: "50"
        run: python -u deriv_bot.py
      - name: Upload Deriv trade log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deriv-trade-log-${{ github.run_id }}
          path: deriv_trades.log
          if-no-files-found: warn
